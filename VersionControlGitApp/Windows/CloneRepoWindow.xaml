<Window x:Class="VersionControlGitApp.CloneRepoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VersionControlGitApp"
        mc:Ignorable="d"
        Title="CloneRepoWindow" WindowStyle="ToolWindow"
        MinHeight="200" MinWidth="400" Height="200" Width="400" 
        WindowStartupLocation="CenterOwner" Background="{StaticResource BlackColor}">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="12"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="12"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="12"/>
        </Grid.RowDefinitions>
        
        <Label Content="Select repository or insert URL:" FontSize="12" 
               Grid.Column="1" Grid.Row="2" Grid.ColumnSpan="2"
               HorizontalAlignment="Center" VerticalAlignment="Center"
               Foreground="{StaticResource WhiteTextColor}"/>

        <ComboBox x:Name="ExternalRepoComboBox" SelectionChanged="ExternalRepoComboBox_SelectionChanged" 
                  FontSize="16" Margin="8,4,8,4"
                  Grid.Column="3" Grid.Row="2" Grid.ColumnSpan="2"/>

        <TextBox x:Name="URL" FontSize="16" 
                 Margin="8,4,8,4" BorderThickness="1"
                 Grid.Column="1" Grid.Row="3" Grid.ColumnSpan="4"
                 Background="{StaticResource DarkBackgroundColor}"  
                 Foreground="{StaticResource WhiteTextColor}"
                 BorderBrush="{StaticResource AccentsColor}"/>

        <Button Click="CloneRepository" Content="Clone" 
                Grid.Column="2" Grid.Row="4" Grid.ColumnSpan="2"
                HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                HorizontalContentAlignment="Center" VerticalContentAlignment="Center"
                FontSize="14" FontWeight="SemiBold"
                Margin="8,4,8,4"
                Background="{StaticResource AccentsColor}" 
                Foreground="{StaticResource BlackColor}"/>
    </Grid>
</Window>
